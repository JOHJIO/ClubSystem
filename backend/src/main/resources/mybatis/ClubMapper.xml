<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.club.mapper.ClubMapper">
    <resultMap id="BaseResultMap" type="Club">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category" property="category"/>
        <result column="leader" property="leader"/>
        <result column="contact" property="contact"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, name, category, leader, contact, description, status, create_time FROM club
        <where>
            <if test="keyword != null and keyword != ''">
                name LIKE CONCAT('%', #{keyword}, '%') OR category LIKE CONCAT('%', #{keyword}, '%') OR leader LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, name, category, leader, contact, description, status, create_time FROM club WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO club (name, category, leader, contact, description, status)
        VALUES (#{name}, #{category}, #{leader}, #{contact}, #{description}, #{status})
    </insert>

    <update id="update">
        UPDATE club
        <set>
            name = #{name},
            category = #{category},
            leader = #{leader},
            contact = #{contact},
            description = #{description},
            status = #{status},
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM club WHERE id = #{id}
    </delete>
</mapper>
