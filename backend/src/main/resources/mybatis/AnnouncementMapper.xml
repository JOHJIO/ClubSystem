<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.club.mapper.AnnouncementMapper">
    <resultMap id="BaseResultMap" type="Announcement">
        <id column="id" property="id"/>
        <result column="club_id" property="clubId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="publish_time" property="publishTime"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, club_id, title, content, publish_time, status FROM announcement
        <where>
            <if test="keyword != null and keyword != ''">
                title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, club_id, title, content, publish_time, status FROM announcement WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcement (club_id, title, content, publish_time, status)
        VALUES (#{clubId}, #{title}, #{content}, #{publishTime}, #{status})
    </insert>

    <update id="update">
        UPDATE announcement
        <set>
            club_id = #{clubId},
            title = #{title},
            content = #{content},
            publish_time = #{publishTime},
            status = #{status},
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM announcement WHERE id = #{id}
    </delete>
</mapper>
